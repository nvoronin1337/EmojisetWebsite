function GoToTeam() {
	let url = location.href.replace('docs', '') + "#team"
	location.href = url
}

function GoToContact() {
	let url = location.href.replace('docs', '') + "#contact"
	location.href = url
}